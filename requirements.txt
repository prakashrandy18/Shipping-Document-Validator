# Shipping Document Validator - Required Libraries
# ================================================
# 
# Install all required packages with:
#     pip install -r requirements.txt
#
# Note: tkinter is built into Python and doesn't need installation

# PDF text extraction library
# LLM Integration
google-genai
python-dotenv>=0.9.0
flask
gunicorn
